# Installed as /etc/sysusers.d/ollama.conf, which masks the packaged file of the same name.
# A fixed UID/GID keeps ownership of /mnt/nomad/ollama stable across every host.
g ollama 614
u! ollama 614:614 "ollama user" /var/lib/ollama
